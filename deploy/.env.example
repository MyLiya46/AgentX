# ============================================
# Redis 缓存配置（可选）
# ============================================
# Redis 用于缓存 Agent 聊天中的文件内容，避免重复从 OSS 下载
# 设置 REDIS_ENABLED=false 可禁用 Redis，系统自动降级为无缓存模式
REDIS_ENABLED=true
REDIS_HOST=redis
REDIS_PORT=6379

# ============================================
# 阿里云 OSS 对象存储配置
# ============================================
# 请从阿里云 RAM 访问控制获取 AccessKey
# 生产环境建议使用 RAM 子账号 + 最小权限策略
OSS_ENDPOINT=oss-cn-guangzhou.aliyuncs.com
OSS_ACCESS_KEY=your_access_key_id
OSS_SECRET_KEY=your_access_key_secret
OSS_BUCKET_NAME=example-of-oss
OSS_REGION=cn-guangzhou
OSS_DOMAIN=https://example-of-oss.oss-cn-guangzhou.aliyuncs.com/

# ============================================
# GitHub SSO OAuth 配置
# ============================================
# 用于 GitHub 第三方登录，请在 GitHub Developer Settings 创建 OAuth App
# 回调地址需设置为: <BACKEND_URL>/api/sso/github/callback
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret
GITHUB_REDIRECT_URI=https://yourdomain.com/api/sso/github/callback
